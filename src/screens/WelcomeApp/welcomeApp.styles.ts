import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  pagerView: {
    flex: 1,
  },
  image: {
    marginTop: 20,
    width: 'auto',
    height: 300,
  },
});
export default styles;
